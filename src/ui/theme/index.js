import colors from './partials/colors';
import { fonts } from './partials/fonts';
import zIndex from './partials/zIndex';
import device from './partials/device';
import utility from './partials/utility';

const theme = {
  colors,
  device,
  fontset: fonts,
  zIndex,
  utility
};

export default theme;